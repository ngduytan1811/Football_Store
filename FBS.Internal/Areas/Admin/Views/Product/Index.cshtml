@using FBS.API.Responses.Base
@using FBS.Application.DataTranferObjects.Products

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var products = (BaseTableResponse<ProductDto>)ViewData["Products"];
}

<div class="container-fluid basic_table">
    <div class="text-end mb-3">
        <a asp-action="Create" asp-controller="Product" class="btn btn-primary">Thêm mới sản phẩm</a>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="card px-4 ">
                <div class="table-responsive theme-scrollbar py-4">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Tên sản phẩm</th>
                                <th scope="col">Danh mục</th>
                                <th scope="col">Đơn giá</th>
                                <th scope="col">Kích thước</th>
                                <th scope="col">Màu</th>
                                <th scope="col">Trạng thái</th>
                                <th scope="col">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach(var product in products.Items)
                            {
                                <tr>
                                    <th scope="row">@product.Index</th>
                                    <td>@product.Name</td>
                                    <td>@(product.CategoryName ?? "--")</td>
                                    <td>@(product.Price.HasValue? product.PriceString: "--")</td>
                                    <td>@string.Join(", ", product.Sizes)</td>

                                    <td>@product.Color</td>
                                    <td>@product.StatusName</td>
                                    <td>
                                        <a class="text-primary" asp-action="Edit" asp-controller="Product" asp-route-id="@product.Id">
                                            <i data-feather="edit-2"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>



